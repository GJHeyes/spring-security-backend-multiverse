spring:
  datasource:
    url: jdbc:mysql://localhost:3306/krusty-krab?useSSL=false
    username: root
    password: MyNewPass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 7000
